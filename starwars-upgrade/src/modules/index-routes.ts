import { FastifyInstance } from "fastify";
//import peopleRoute from './people/people-route';
//import planetRoute from './planet/planet-route';
//import starshipRoute from './starship/starship-route';

const routerApi = async (fastify: FastifyInstance) => {
  //fastify.register(filmRoute, { prefix: '/film' });
  //fastify.register(peopleRoute, { prefix: '/people' });
  //fastify.register(planetRoute, { prefix: '/planet' });
  //fastify.register(starshipRoute, { prefix: '/starship' });
};

export default routerApi;
